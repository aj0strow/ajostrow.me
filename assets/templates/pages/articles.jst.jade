.articles
ul.pager
  - var prev = page - 1
  - var next = page + 1
  if prev >= 0
    - var link = '/articles' + (prev == 0 ? '' : '/' + prev)
    li.previous
      a(href="#{link}") &larr; prev
  if next <= 3
    li.next
      a(href="/articles/#{next}") next &rarr;
